{
  "name": "harutool",
  "version": "1.0.0",
  "private": true,
  "description": "매일 쓰는 생활·업무 도구 모음",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js && node --check test/smoke.test.js",
    "test": "node --test test/smoke.test.js",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=18"
  }
}
